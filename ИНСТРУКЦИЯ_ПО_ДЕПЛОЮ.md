# 🚀 Инструкция по деплою Telegram Recipe App

## 📋 Обзор проекта 2

Этот проект состоит из двух частей:
1. **Python скрипт для извлечения рецептов** (`recipe_extractor.py`) - уже выполнен
2. **Telegram Mini App** (веб-приложение на Next.js) - готово к деплою

### Что уже есть:
- ✅ 399 извлеченных рецептов в `recipes_extracted.json`
- ✅ Полностью готовое веб-приложение на Next.js + TypeScript + TailwindCSS
- ✅ Настроенная конфигурация для статического экспорта
- ✅ Готовая настройка для Vercel

---

## 🎯 Быстрый старт (5 минут)

### Шаг 1: Установка зависимостей

```bash
cd telegram-recipe-app
npm install
```

### Шаг 2: Локальное тестирование

```bash
npm run dev
```

Откройте http://localhost:3000 в браузере и проверьте работу приложения.

### Шаг 3: Деплой на Vercel

```bash
# Установите Vercel CLI глобально (один раз)
npm install -g vercel

# Залогиньтесь в Vercel
vercel login

# Задеплойте проект
vercel
```

После завершения вы получите URL вида: `https://ваш-проект.vercel.app`

---

## 📝 Подробная инструкция по деплою

### Вариант А: Через Vercel CLI (рекомендуется)

#### 1. Предварительные требования

- Node.js 18+ установлен
- Аккаунт на Vercel (бесплатный): https://vercel.com/signup

#### 2. Сборка проекта

```bash
cd telegram-recipe-app

# Установите зависимости
npm install

# Проверьте локально
npm run dev
# Откройте http://localhost:3000

# Соберите продакшн версию
npm run build
```

Сборка создаст статические файлы в папке `out/`.

#### 3. Деплой

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в аккаунт
vercel login

# Деплой в preview
vercel

# Деплой в продакшн
vercel --prod
```

При первом деплое Vercel спросит:
- **Set up and deploy**: Yes
- **Which scope**: Выберите ваш аккаунт
- **Link to existing project**: No
- **Project name**: telegram-recipe-app (или свое название)
- **In which directory**: . (текущая директория)
- **Override settings**: No

#### 4. Получите URL

После успешного деплоя вы получите:
- **Preview URL**: https://telegram-recipe-app-xxx.vercel.app
- **Production URL**: https://telegram-recipe-app.vercel.app

---

### Вариант Б: Через GitHub + Vercel Dashboard

#### 1. Загрузите проект на GitHub

```bash
cd telegram-recipe-app

# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: Telegram Recipe App"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш-username/telegram-recipe-app.git

# Загрузите на GitHub
git push -u origin main
```

#### 2. Подключите репозиторий к Vercel

1. Перейдите на https://vercel.com/new
2. Нажмите **"Import Git Repository"**
3. Выберите ваш репозиторий
4. Vercel автоматически определит Next.js
5. Нажмите **"Deploy"**

#### 3. Настройки (автоматические)

Vercel автоматически использует настройки из `vercel.json`:
- **Framework**: Next.js
- **Build Command**: `npm run build`
- **Output Directory**: `out`

---

### Вариант В: Ручной деплой (Drag & Drop)

Если не хотите использовать CLI или GitHub:

1. Соберите проект локально:
```bash
npm run build
```

2. Перейдите на https://vercel.com/new
3. Перетащите папку `out/` в окно браузера
4. Vercel автоматически задеплоит

---

## 🤖 Настройка Telegram бота

### Шаг 1: Создайте бота (если еще не создан)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте команду: `/newbot`
4. Введите **имя бота**: `Recipe Catalog Bot` (или свое)
5. Введите **username**: `my_recipe_bot` (должен заканчиваться на `bot`)
6. Сохраните токен бота (не нужен для Mini App, но сохраните на будущее)

### Шаг 2: Создайте Web App

1. В @BotFather отправьте: `/newapp`
2. Выберите вашего бота
3. Заполните:
   - **Title**: `Каталог рецептов`
   - **Short name**: `recipes` (маленькие буквы, без пробелов)
   - **Description**: `Каталог из 399+ рецептов от канала КЕРЦМАН`
   - **Photo**: Загрузите картинку еды (640x360 пикселей)
   - **Web App URL**: Вставьте ваш Vercel URL (например: `https://telegram-recipe-app.vercel.app`)

### Шаг 3: Настройте кнопку меню

1. В @BotFather отправьте: `/setmenubutton`
2. Выберите вашего бота
3. Выберите тип: **Web App**
4. Введите текст кнопки: `🍳 Открыть каталог`
5. Введите Web App URL: Ваш Vercel URL

### Шаг 4: Протестируйте

1. Откройте Telegram (лучше на телефоне)
2. Найдите вашего бота
3. Нажмите кнопку **"Menu"** (слева внизу) или отправьте `/start`
4. Ваше приложение должно открыться! 🎉

---

## 🌐 Дополнительные настройки

### Использование собственного домена

1. В Vercel Dashboard:
   - Перейдите в **Settings → Domains**
   - Добавьте ваш домен (например: `recipes.yourdomain.com`)
   - Следуйте инструкциям по настройке DNS
2. Обновите Web App URL в @BotFather на новый домен

### Переменные окружения (опционально)

Проект работает полностью статически, переменные не требуются.

Если нужна аналитика, создайте `.env.local`:

```bash
NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
```

И добавьте в `.gitignore`:
```bash
echo ".env.local" >> .gitignore
```

---

## 🔧 Команды для работы

```bash
# Разработка
npm run dev          # Запустить dev сервер (http://localhost:3000)

# Продакшн
npm run build        # Собрать проект
npm run start        # Запустить продакшн сервер (после build)

# Проверка кода
npm run lint         # Проверить качество кода

# Деплой
vercel               # Деплой в preview
vercel --prod        # Деплой в продакшн
```

---

## 🧪 Тестирование

### Локальное тестирование

```bash
# В режиме разработки
npm run dev
# Откройте: http://localhost:3000

# Тест продакшн сборки
npm run build
npx serve out
# Откройте: http://localhost:3000
```

### Тестирование в Telegram (на этапе разработки)

Используйте ngrok для доступа к localhost из Telegram:

```bash
# Установите ngrok
npm install -g ngrok

# Запустите dev сервер
npm run dev

# В другом терминале
ngrok http 3000

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# Временно обновите URL в @BotFather для тестирования
```

---

## 📊 Мониторинг и аналитика

### Vercel Analytics (бесплатно)

1. Перейдите в Vercel Dashboard → Ваш проект → Analytics
2. Включите **"Vercel Analytics"**
3. Смотрите статистику посещений, производительность и т.д.

### Пользовательская аналитика

Можно добавить Google Analytics, Yandex.Metrika и т.д. в `src/app/layout.tsx`.

---

## 🐛 Решение проблем

### Сборка не работает

```bash
# Очистите кеш
rm -rf .next out node_modules

# Переустановите зависимости
npm install

# Пересоберите
npm run build
```

### Web App не открывается в Telegram

1. **Проверьте HTTPS**: Telegram требует HTTPS
   - ✅ Vercel автоматически предоставляет HTTPS

2. **Проверьте URL в BotFather**:
   - Должен точно совпадать с задеплоенным (с https://)

3. **Очистите кеш Telegram**:
   - Settings → Data and Storage → Clear Cache

4. **Проверьте консоль браузера**:
   - Откройте Web App
   - Нажмите F12 → Console
   - Посмотрите на ошибки

### Стили не загружаются

Проверьте, что в `next.config.js` есть:
```javascript
{
  output: 'export',
  images: { unoptimized: true }
}
```

### Кнопка "Назад" не работает

Убедитесь, что в `src/app/layout.tsx` подключен Telegram SDK:
```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

---

## 🔄 Обновление проекта

### Обновление данных рецептов

1. Отредактируйте `src/data/recipes_extracted.json`
2. Пересоберите: `npm run build`
3. Задеплойте: `vercel --prod`

### Обновление кода

```bash
# Внесите изменения в код
git add .
git commit -m "Update: описание изменений"
git push

# Если используете GitHub интеграцию, Vercel автоматически задеплоит
# Или вручную:
vercel --prod
```

---

## ✅ Чеклист перед деплоем

- [ ] Все рецепты в `src/data/recipes_extracted.json`
- [ ] `npm install` выполнен успешно
- [ ] `npm run build` работает без ошибок
- [ ] `npm run lint` не показывает ошибок
- [ ] Протестировано локально: `npm run dev`
- [ ] Telegram бот создан
- [ ] Web App URL настроен в @BotFather
- [ ] Кнопка меню настроена
- [ ] Протестировано в Telegram (на мобильном)

---

## 📁 Структура проекта

```
telegram-recipe-app/
├── src/
│   ├── app/              # Страницы (Next.js App Router)
│   ├── components/       # React компоненты
│   ├── lib/              # Утилиты (Telegram SDK, хелперы)
│   ├── types/            # TypeScript типы
│   └── data/             # recipes_extracted.json (данные рецептов)
├── public/               # Статические файлы
├── package.json          # Зависимости
├── next.config.js        # Настройки Next.js
├── vercel.json           # Настройки Vercel
└── README.md            # Полная документация (на английском)
```

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Работающее Telegram Mini App
- ✅ Автоматический деплой при каждом push (если через GitHub)
- ✅ HTTPS и CDN от Vercel
- ✅ 399 рецептов доступны в боте

### Ваше приложение доступно по адресу:
```
https://ваш-проект.vercel.app
```

Пользователи могут получить к нему доступ через вашего Telegram бота! 🚀

---

## 📞 Помощь и документация

**Исходные данные**: Telegram канал [@kerzmaneat](https://t.me/kerzmaneat)  
**Количествo рецептов**: 399  
**Технологии**: Next.js 14 + TypeScript + TailwindCSS

**Полезные ссылки:**
- [Telegram Mini Apps документация](https://core.telegram.org/bots/webapps)
- [Vercel документация](https://vercel.com/docs)
- [Next.js документация](https://nextjs.org/docs)

**Файлы с документацией в проекте:**
- `DEPLOYMENT.md` - подробная инструкция (на английском)
- `QUICKSTART.md` - быстрый старт (на английском)
- `README.md` - полная документация проекта
- `PROJECT_SUMMARY.md` - обзор проекта извлечения рецептов

---

**Дата создания**: 22 ноября 2025  
**Статус проекта**: ✅ Готов к деплою  
**Последнее обновление**: 22 ноября 2025

**Приятного использования! 🍳👨‍🍳**
